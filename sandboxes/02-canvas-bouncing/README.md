# 02-canvas-bouncing

Full-viewport `<canvas>` with DPR-aware sizing, a `requestAnimationFrame` loop, and a small FPS HUD. Scaffolding for any canvas/simulation experiment.

```bash
bun dev 02-canvas-bouncing
```
