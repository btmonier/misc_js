# Identicon avatars

Generate GitHub-style default profile avatars: SHA-1–seeded symmetric 5×5 blocks on pastel backgrounds. Same username always yields the same image.

## Run

```bash
bun dev 09-identicon-avatars
```

## Add a dependency

```bash
bun add --filter @sandbox/09-identicon-avatars <pkg>
```
